import React from 'react'

const Card = ({courses}) => {
  return (
    <div>
       <div>
        <img src={courses.image.url}></img>

        <div>
            <button>

            </button>
        </div>
       </div>
    </div>
  )
}

export default Card
